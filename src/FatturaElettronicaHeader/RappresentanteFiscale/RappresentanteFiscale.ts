import { RappresentanteFiscale as IRappresentanteFiscale } from '../../Common/RappresentanteFiscale';

/**Dati relativi al rappresentante fiscale del cedente / prestatore. */
export interface RappresentanteFiscale extends IRappresentanteFiscale {
    _none?: void;
}
